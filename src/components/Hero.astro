---
---

<section id="home" class="hero">
  <div class="hero-tux" aria-hidden="true">
    <pre>
        .--.
       |o_o |
       |:_/ |
      //   \ \
     (|     | )
     /'\_   _/`\
     \___)=(___/</pre>
  </div>
  <div class="hero-grid">
    <div class="hero-content">
      <p class="hero-greeting animate-on-scroll"><span class="prompt">$</span> echo "Hey! Folks"<span class="cursor">_</span></p>
      <h1 class="hero-name animate-on-scroll">I'm Veera Kumar<span class="prompt">.</span></h1>
      
      <div class="hero-role-container animate-on-scroll">
        <span class="prompt">$</span> whoami → <span id="cycling-role"></span><span class="cursor">_</span>
      </div>
      
      <div class="hero-socials animate-on-scroll">
        <a href="https://www.linkedin.com/in/TheVeeraKumar" target="_blank" rel="noopener noreferrer" class="social-pill" aria-label="LinkedIn">
          <svg viewBox="0 0 24 24"><path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 01-2.063-2.065 2.064 2.064 0 112.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/></svg>
          LinkedIn
        </a>
      </div>
      
      <a href="#about" class="hero-cta animate-on-scroll"> More About Me</a>
    </div>
    
    <div class="hero-visual animate-on-scroll">
      <code>
  ╭──────────────────────╮
  │  VeeraKumar-OS       │
  │  v2026.1             │
  ├──────────────────────┤
  │  ████░░░░  80% AWS   │
  │  █████████░ 90% NET  │
  │  ████████░░ 80% SYS  │
  │  ██████░░░░ 60% DEV  │
  ├──────────────────────┤
  │  > status: online    │
  │  > mode: awesome     │
  ╰──────────────────────╯</code>
    </div>
  </div>
</section>

<script>
  const observer = new IntersectionObserver((entries) => {
    entries.forEach((entry, index) => {
      if (entry.isIntersecting) {
        setTimeout(() => {
          entry.target.classList.add('animated');
        }, index * 100);
        observer.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });

  document.querySelectorAll('.animate-on-scroll').forEach(el => {
    observer.observe(el);
  });

  const roles = ['Network Engineer', 'IT Consultant', 'Cloud Enthusiast', 'Traveller'];
  let roleIndex = 0, charIndex = 0, isDeleting = false;

  function typeRole() {
    const current = roles[roleIndex];
    const el = document.getElementById('cycling-role');
    if (!el) return;
    
    if (isDeleting) {
      el.textContent = `[ ${current.substring(0, charIndex - 1)} ]`;
      charIndex--;
    } else {
      el.textContent = `[ ${current.substring(0, charIndex + 1)} ]`;
      charIndex++;
    }
    
    let speed = isDeleting ? 40 : 60;
    
    if (!isDeleting && charIndex === current.length) {
      speed = 1800;
      isDeleting = true;
    } else if (isDeleting && charIndex === 0) {
      isDeleting = false;
      roleIndex = (roleIndex + 1) % roles.length;
      speed = 500;
    }
    
    setTimeout(typeRole, speed);
  }

  setTimeout(typeRole, 1000);
</script>
